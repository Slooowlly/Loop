# Build outputs (regenerated by CMake / MSVC — must be rebuilt alongside the app)
build/
*.exe
*.dll
*.obj
*.lib
*.exp
*.pdb
*.ilk

# Headers do OpenXR clonados pro build sem CMake (veja o README) — não são nossos.
src/OpenXR-SDK/
